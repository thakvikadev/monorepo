export enum DatabaseSchema {
    PUBLIC = 'public',
    LEAVE = 'leave',
    APPROVAL = 'approval',
}
export enum FileExtension {
    CSV = 'csv', // Comma-separated values
    PDF = 'pdf', // Portable Document Format
    XLSX = 'xlsx', // Excel Microsoft Office Open XML Format Spreadsheet file Computing
}
